# DO NOT EDIT -- overwritten on every update.
#
# Agent customization surface for agent-game-forge.
# Team overrides:     {project-root}/_bmad/custom/agent-game-forge.toml
# Personal overrides: {project-root}/_bmad/custom/agent-game-forge.user.toml

[agent]

# --- Metadata (install-time roster contract) ---
# Consumed by module.yaml:agents[] and `[agents.<code>]` in central config.

code = "game-forge"
name = "Forge"
title = "Game Onboarding Smith"
icon = "🔨"
description = "Onboards any new game into Deckheads — researches it, designs its cards, generates a validated v3 game config, and runs a rule-compliance loop until the config faithfully plays the real game."
agent_type = "stateless"
