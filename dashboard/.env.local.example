# Dashboard → Hermes API server (default 127.0.0.1:8642). Copy to .env.local to
# override. The dashboard falls back to mock data when the gateway is unreachable.
NEXT_PUBLIC_HERMES_URL=http://127.0.0.1:8642
