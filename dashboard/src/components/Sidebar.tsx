"use client";

// Sidebar — RETIRED in Wave 3.
//
// Navigation has moved to the top centered Dock (components/ui/Dock.tsx).
// This file is kept as a null export to avoid breaking any stale imports
// during the Wave 3 migration. Safe to delete once all consumers are updated.

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function Sidebar(_props: Record<string, unknown>) {
  return null;
}
