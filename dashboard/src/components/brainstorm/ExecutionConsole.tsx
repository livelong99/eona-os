// Moved to the shared toolkit (single source of truth). Re-exported so existing
// `@/components/brainstorm/ExecutionConsole` imports keep working.
export { ExecutionConsole, ALL_AGENTS } from "@/components/toolkit/ExecutionConsole";
